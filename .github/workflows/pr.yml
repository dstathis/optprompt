name: Pull Request 

on: [pull_request, push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Flake8
      run: flake8
    - name: Tests
      run: echo "There are no tests yet"
